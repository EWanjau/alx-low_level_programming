#include "main.h"

/**
 * set_string - sets a pointer to a character
 * @s: the pointer tommemory location
 * @to: the character 
 * Return: the character
 */
void set_string(char **s, char *to)
{
	*s = to;
}
