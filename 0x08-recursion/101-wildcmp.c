#include "main.h"

/**
 *wildcmp - checks for matching strings
 *@s1: first input
 *@s2: 2nd string input
 *Return: 1 for a match
 */
int wildcmp(char *s1, char *s2)
{
  if (*s1 != '\0')
  {
    return (0);
  }
  else
  {
      
  }
}
