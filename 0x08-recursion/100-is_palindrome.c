#include "main.h"

/**
 *is_palindrome - checks for palindromes in strings
 *@s: input
 *Return: 1 for palindrome and 0 if else
 */
int is_palindrome(char *s)
{
  if (*s == " ")
    return (1);
  else
    return (-1);
}
