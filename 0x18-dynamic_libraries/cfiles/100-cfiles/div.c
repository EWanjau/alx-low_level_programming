#include "main.h"

/**
 *div - gets result of division
 *@a: 1st parameter to be used
 *@b: divisor
 * Return: the result
 */
int div(int a, int b)
{
	int result;

	result = a / b;
	return (result);
}
