#include "main.h"

/**
 *mod - gets result of modulus
 *@a: 1st parameter to be used
 *@b: parameter condition
 * Return: the result
 */
int mod(int a, int b)
{
	int result;

	result = a % b;
	return (result);
}
