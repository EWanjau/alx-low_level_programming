#include "main.h"

/**
 *sub - gets result of subtraction
 *@a: 1st parameter to be used
 *@b: parameter to be subtracted
 * Return: the result
 */
int sub(int a, int b)
{
	int result;

	result = a - b;
	return (result);
}
